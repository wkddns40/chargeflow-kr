# Ingestion

Near-term ingestion uses login-free public file downloads only.

Planned adapters:

- Korea Environment Corporation charger location/operation file dataset.
- Jeju charging history file datasets.
- Regional CSV files when downloadable without login.

Each adapter should preserve:

- source URL or manual acquisition note,
- snapshot date,
- raw file hash,
- row-level validation errors.
